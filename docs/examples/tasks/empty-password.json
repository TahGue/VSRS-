{
  "instruction": "Fix the login bug where an empty password is accepted by validate_password. The function should reject empty strings and None values.",
  "type": "bugfix",
  "risk": "low",
  "acceptance_criteria": [
    "validate_password('') returns False",
    "validate_password(None) returns False",
    "validate_password('valid') returns True",
    "All existing tests still pass"
  ]
}
